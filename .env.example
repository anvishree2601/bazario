# Frontend (Vite)
VITE_API_URL=http://localhost:5000

# Backend / MySQL (also used when running API from backend/)
DB_HOST=localhost
DB_USER=bazario_user
DB_PASSWORD=password123
DB_NAME=bazario
JWT_SECRET=bazario_01
PORT=5000
